# frozen_string_literal: true
source "https://rubygems.org"

gem 'builder'

group 'development' do
  gem 'pry'
  gem 'pry-debugger' if RUBY_VERSION < '2.0.0'
  gem 'pry-byebug' if RUBY_VERSION >= '2.0.0'
end
